---
layout: layouts/layout.njk
title: 
---

<h2>Dear diary</h2>
<p>Check back here for more thinking, public responses to queries, and where I'll be out and about to meet up.</p>

{% set posts = collections.posts | reverse %}
{% for post in posts %}
  <article>
    <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
    <p class="meta">{{ post.date | date("MMMM D, YYYY") }}</p>
    <p>{{ post.data.excerpt }}</p>
  </article>
{% endfor %}
